
export default {
  // Cell size for tiles and entities
  cellSize: 10,

  // world viewport
  viewportSize: [64, 48],
  viewportScale: 1,

  // World size for generation and rendering test,
  worldSize: Math.pow(2, 12),
  // worldSize: 48,

  // Camera offset around dude
  dudeCameraOffset: 10,

  // How quickly the key repeat happens
  keyRepeat: 1000 / 20
}
