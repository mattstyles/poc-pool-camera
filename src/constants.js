
export default {
  // Cell size for tiles and entities
  cellSize: 10,

  // World size for generation and rendering test,
  // worldSize: Math.pow(2, 12),
  worldSize: 48,

  // Camera offset around dude
  dudeCameraOffset: 10
}
